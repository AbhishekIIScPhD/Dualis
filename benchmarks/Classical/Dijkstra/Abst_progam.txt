Dijkstra (G, s):
    FibonnacciHeap fh;

    assume (N > 0)

    assume (s >= 0 && s < N)
    allReachable = false
    
    min1, max1, SReachable1, OReachable1, visitedNodes1, connected1 = fh.initialize(N, s, min, max, SReachable, OReachable, visitedNodes); //set all the distances from source-source (s) to 0 and others to infinity

    while (fh.isEmpty()) {
    	  min, max, u = fh.extractMin()
	  min1, max1 = fh.updateDistance(u, min, max)
    }

assert( min > 0)
=======================================

Dijkstra (G, s):
    FibonnacciHeap fh;

    assume (N > 0)

    assume (s >= 0 && s < N) // source
    allReachable = false
    visitedNodes = 0
    sumDis = 0
    maxDis = 0
    
     sumDis, maxDis, visitedNodes1 = fh.initialize(N, s, maxDis, sumDis, visitedNodes); //set all the distances from source-source (s) to 0 and others to infinity

    while (visiNodes < N) {
    	  u = fh.extractMin()
	  visitedNodes1, sumDis1, maxDis1 = fh.updateDistance(u, visitedNodes, sumDis, maxDis) // looks at the adjacent nodes and updates the distances
    }

assert( maxDis <= sumDis)

====================================
Checks nodes in a graph for shortest path computation is indeed the sum of visited and unreachable nodes

unreachable, nodes with minimum distance as INFINITY

Dijkstra (G, s):
    FibonnacciHeap fh;

    assume (N > 0)

    assume (s >= 0 && s < N)
    allReachable = false
    
    min1, max1, unReachable, visitedNodes1 = fh.initialize(N, s, min, max, unReachable, visitedNodes); //set all the distances from source-source (s) to 0 and others to infinity

    while (!fh.isEmpty()) {
    	  u = fh.extractMin()
	  visitedNodes1, unReachable1 = fh.updateDistance(u, visitedNodes, unReachable) // looks at the adjacent nodes and updates the distances
    }

assert( N = (visitedNodes + unReachable))

=====================================
Check if for a given u and v, if are visited in v after u then the distances to v should be greater then u

Dijkstra (G, s):
    FibonnacciHeap fh;

    assume (N > 0)

    assume (s >= 0 && s < N)

    assume (u >= 0 && u < N)

    assume (v >= 0 && v < N)

    visitedNodes := 0
    
    visitedNodes1 = fh.initialize(N, s, visitedNodes); //set all the distances from source-source (s) to 0 and others to infinity

    while (visitedNodes < (N-1) ) {
    	  x, xmin, visitedNodes1 = fh.extractMin(visitedNodes)
	  if (x == u){
	     minu := xmin 
	  } else if (x == v){
	     minv := xmin
	  }
	  fh.updateDistance(x) // looks at the adjacent nodes and updates the distances
    }

assert(u < v ==> minu <= minv)
