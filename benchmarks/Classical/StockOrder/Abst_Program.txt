len = 0;
minDiff = MAX;

while (*) {
    stock = *;
    order = *;

    if(stock >= 0 && order >= 0 && order <= stock) {
        (len', minDiff') = addStockOrder(stock, order, len, minDiff)
    }
}

assert(len >= 0 ==> minDiff >= 0)

======================================
since values to be inserted are in the range from [0, 11), lets have minDiff = 11
