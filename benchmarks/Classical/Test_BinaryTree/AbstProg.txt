bool isEmpty = true;
int min = INT_MAX;


while(*) {
    int n;   
    if (n >= 0) { // only insert positive numbers
        b.insert(n, min, min1, isEmpty, isEmpty1);
    }
}

int v;
assume(v < 0);
search(v, min, isEmpty, ret)
assert (ret == false); // didn't put in any negative numbers
